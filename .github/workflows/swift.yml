name: Test

on: [push, pull_request]

jobs:
  test:
    name: test
    runs-on: macOS-latest
    env:
        XCODE_VERSION: 11.1

    steps:
      - name: Checkout
        uses: actions/checkout@v1
      - name: install dependencies
        run: |
          brew install swiftlint
      - name: Select XCode
        run: sudo xcode-select --switch /Applications/Xcode_${{ job.env.XCODE_VERSION }}.app
      - name: update node
        uses: actions/setup-node@v1
        with:
          node-version: '11.x'
      - name: Cache carthage
        uses: actions/cache@v1
        with:
          path: Carthage
          key: ${{ $XCODE_VERSION }}-carthage-${{ hashFiles('**/Cartfile.resolved') }}
          restore-keys: |
            ${{ $XCODE_VERSION }}-carthage-
      - name: Run bootstrap script
        run: ./bootstrap.sh -ci
      - name: Run tests
        run: |
          set -o pipefail
          fastlane ios test
